import { StyleSheet } from 'react-native'
import { RED_COLOR } from '../../../../assets/styles/common.style'

export const ValidationMessageStyles = StyleSheet.create({
  errorMessage: {
    color: RED_COLOR,
    fontSize: 16,
    marginBottom: 30
  }
})
