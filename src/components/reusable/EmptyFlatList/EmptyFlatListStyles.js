import { StyleSheet } from 'react-native'

export const EmptyFlatListStyles = StyleSheet.create({
  //Container
  cartEmpty: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },

  //Text
  emptyList: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    textAlign: 'center',
    fontSize: 22
  }
})
